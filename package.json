{
  "dependencies": {
    "factorio-blueprint": "^2.6.5",
    "priorityqueuejs": "^2.0.0"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "watchify": "^4.0.0"
  },
  "scripts": {
    "watch": "watchify src/main.js -o public/bundle.js --debug --verbose",
    "build": "browserify src/main.js -o public/bundle.js"
  }
}
